spring:
  application:    name: config-server
  cloud:
    config:      server:        git:
          uri: https://github.com/Hartford200s/config-server-properties.git
          
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/ #Eureka Server的位址